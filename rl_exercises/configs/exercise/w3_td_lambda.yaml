# @package _global_
defaults:
  - ../agent: td_lambda

env_name: RandomWalkTDEnv
env_kwargs:
  horizon: 100
training_steps: 10000
batch_size: 0
buffer_cls: SimpleBuffer
buffer_kwargs: {}
eval_every_n_steps: 1000
n_eval_episodes: 30
